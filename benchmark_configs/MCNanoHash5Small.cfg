CONSTANTS
  CalculateHash <- CalculateHashImpl
  Hash = {h1, h2, h3, h4, h5}
  NoHashVal = NoHashVal
  PrivateKey = {prv1}
  PublicKey = {pub1}
  Node = {n1}
  GenesisBalance = 1
  NoBlockVal = NoBlockVal

CONSTANTS
  NoHash = [Nano]NoHashVal
  NoBlock = [Nano]NoBlockVal

VIEW
  View

SPECIFICATION
  Spec

INVARIANTS
  TypeInvariant
  SafetyInvariant
