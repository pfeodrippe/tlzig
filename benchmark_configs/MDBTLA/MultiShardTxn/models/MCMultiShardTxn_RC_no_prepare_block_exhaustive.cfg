\* Exhaustive benchmark copy of vendor/MDBTLA/MultiShardTxn/models/MCMultiShardTxn_RC_no_prepare_block.cfg.
\* Keep constants in sync with the upstream config; only deadlock checking is disabled.

INIT InitCatalogConstraintKeysOnDifferentShards
NEXT Next

CONSTANT
    RC = "local"
    IgnorePrepareBlocking = "force"
    IgnoreWriteConflicts = "false"
    Keys = {k1,k2}
    TxId = {t1,t2}
    NoValue = NoValue
    Shard = {s1,s2}
    Router = {r1}
    MaxOpsPerTxn = 2
    Timestamps = {1,2,3}

\* Symmetry and state constraint.
\* SYMMETRY Symmetry
\* INVARIANT ReadCommittedIsolation
CONSTRAINT StateConstraint
CHECK_DEADLOCK FALSE
