\* Diagnostic performance model for measuring invariant-checking cost only.
\* Do not use as a correctness benchmark: this intentionally removes
\* TypeOK/BaitLog/SnapshotIsolation from ShardTxn_small.cfg.
SPECIFICATION
    Spec

CONSTANT
    RC = "linearizable"
    WC = "majority"
    Keys = {k1, k2}
    Values = {t1, t2}
    NoValue = NoValue
    STMTS = 2

PROPERTIES
    Termination

SYMMETRY
    TxIdSymmetric
